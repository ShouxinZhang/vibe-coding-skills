# Human Log：写给人类审阅的任务日志

## 一、人类日志的定位

人类日志是 Agent Workflow 的**审计档案**。

它不参与流水线的自动执行，不被其他 agent 读取，不影响任何门禁的判断。它存在的唯一理由是：当出现问题时，人类能够回溯——agent 做了什么、为什么这样做、哪一步出了岔子。

写好人类日志的标准很简单：**人类在没有其他上下文的情况下，仅凭日志就能还原当时发生的一切。**

---

## 二、核心字段

| 字段 | 问题 | 说明 |
|------|------|------|
| `timestamp` | 什么时候发生的？ | ISO 8601 格式，精确到秒 |
| `prompt` | 用户说了什么？ | 原始用户 prompt，**不做任何改写** |
| `summary` | Agent 做了什么？为什么？ | 叙事性摘要，含关键决策点和理由 |
| `files_modified` | 动了哪些文件？ | 新建 / 修改 / 删除，标注操作类型 |
| `git_op` | 是否有 git 操作？ | yes/no；若有，记录具体 commit 或 push 动作 |
| `gate_results` | 各门禁结果如何？ | 每个门禁的 pass/fail 状态，失败时附原因 |
| `isolated` | 变更范围是否受控？ | 修改是否限定在预期模块内，有无溢出 |
| `backup` | 操作前是否有备份？ | git snapshot、文件备份或其他恢复手段 |
| `integrity_ok` | 任务后工作区完整吗？ | 结构是否正常，文档是否更新 |
| `recoverability_ok` | 结果可回滚吗？ | 若结论有误，是否有明确的还原路径 |
| `error` | 是否出现错误？ | 若有，记录发生位置、错误信息、处理方式 |

---

## 三、日志的结构层次

单条日志记录一次任务，但长期运行需要多个层次支撑：

```
docs/agent-logs/
  ├── YYYY-MM-DD/
  │     └── log-<序号>-<HHMMSS>.md    ← 单条任务日志
  ├── INDEX.md                         ← 日期索引（按天聚合摘要）
  └── AGENT_LOGBOOK.md                 ← 全局日志册（所有任务摘要，最新在前）
```

**单条日志**：完整字段，叙事性摘要，供深度溯源。  
**日期索引**：当天所有任务的标题列表，方便时间线回溯。  
**全局日志册**：一张表，每行一条任务，最新在前，用于快速扫描历史。

---

## 四、写入时机

人类日志遵循严格的写入时序：

**时机一：任务开始，第一次实际操作前**  
写入 `timestamp`、`prompt`、初始工作区快照（git HEAD）。  
这是"意图存档"——即使任务中途崩溃，也有起点记录。

**时机二：任务结束（无论成功或失败）**  
补全所有字段，写入最终状态，导出 INDEX 和 AGENT_LOGBOOK。  
这是任务的"关账"动作，必须在通知用户之前完成。

**时机三：`on-error` Hook 触发**  
单独写入一条错误日志，描述异常发生的现场。不合并进正常任务日志，单独存档。

---

## 五、格式示例

````markdown
# Task Log — 2026-03-06 · #0042

## 基本信息
- **时间**: 2026-03-06T14:32:07+08:00
- **Prompt**: 在 agent_workflow 模块新增 hooks.md，并更新架构文档

## 操作摘要
参考 agents_drone_testing 的 SKILL 结构，新建了 hooks.md，内容涵盖
Hook 的触发模型、五种核心 Hook 类型、设计原则及与门禁的对比表。
同步更新了 docs/architecture/repository-structure.md。

决策点：Hook 类型按触发时机而非功能分类，更符合流水线阅读视角。

## 修改文件
- [新建] BasicKnowledge/agent_workflow/hooks.md
- [修改] docs/architecture/repository-structure.md

## Git 操作
- 未执行 commit（由用户决定提交时机）

## 门禁结果
- architecture-guard: PASS
- workspace-integrity: PASS

## 安全性评估
- isolated: yes（变更限定在 BasicKnowledge/agent_workflow/ 和 docs/）
- backup: yes（git working tree 可回滚至 HEAD）
- integrity_ok: yes
- recoverability_ok: yes

## 错误记录
- 无
````

---

## 六、摘要写作原则

摘要（`summary`）是人类日志中价值最高、也最容易写烂的字段。

**好的摘要回答三个问题：**
1. 做了什么（操作事实）
2. 为什么这样做（关键决策依据）
3. 有没有值得注意的情况（异常、妥协、遗留问题）

**坏的摘要举例：**
- "完成任务" — 没有信息量
- "按照要求修改了代码" — 没有具体内容
- 复制粘贴 prompt 原文 — 没有 agent 的判断和行动

**好的摘要举例：**
> 新增了 hooks.md，重点区分了 Hook 与门禁的职责边界。触发模型部分用 ASCII 流程图替代文字描述，因为视觉结构在这里比文字更清晰。`on-gate-fail` Hook 的重试上限设计参考了 agents_drone_testing 的实践。

---

## 七、防腐烂机制

人类日志最大的风险是**被跳过**。

唯一有效的预防手段：将日志写入纳入 `post-task` Hook，作为任务完成的**必要条件**，与工作区门禁同级执行。

日志未写入 = 任务未完成。

这不是惩罚，这是定义：一个没有可审计记录的 agent 操作，在系统层面等同于从未发生。
