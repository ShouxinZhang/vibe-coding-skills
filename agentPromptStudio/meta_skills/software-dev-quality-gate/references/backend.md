# Backend Gate

后端验证优先证明“业务动作真的发生了”。

## 最小门禁

- 服务或函数能真实启动
- 至少一次真实请求或消息投递
- 验证响应或副作用
- 验证错误路径或边界条件至少一条

## 证据示例

- `curl` / HTTP client 响应
- 队列消息发送与消费日志
- 数据库状态变化
- 输出文件或缓存变化

## 常见错误

- 只跑单元测试，不验证真实请求链路
- 只看 200 状态码，不验证业务结果
- 只看日志，不验证状态变化
