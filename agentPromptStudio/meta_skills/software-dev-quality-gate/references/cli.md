# CLI And Pipeline Gate

CLI、脚本和 pipeline 验证优先证明“命令真的跑完且产物正确”。

## 最小门禁

- 真实执行命令
- 检查退出码
- 检查关键输出
- 检查生成文件或中间产物

## 证据示例

- 标准输出关键行
- 目标文件存在且内容合理
- 运行前后目录差异
- 样本输入到样本输出的闭环

## 常见错误

- 只跑 `--help`
- 只看退出码不看结果
- 没有验证产物内容
