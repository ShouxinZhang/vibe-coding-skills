# Frontend Gate

前端验证优先证明“界面行为符合意图”，不是只看编译通过。

## 最小门禁

- 页面真实加载
- 抓取当前界面状态
- 执行一次与需求相关的交互
- 再次抓取界面状态并对比变化

## 推荐流程

1. 抓初始页面
2. 阅读页面结构、文本、按钮状态
3. 执行点击 / 输入 / 拖拽 / 键盘动作
4. 再次抓取页面
5. 判断变化是否符合开发意图

## 常见错误

- 只看 DOM，不看实际渲染结果
- 连续乱点，不做逐步验证
- 没有前后状态对比
